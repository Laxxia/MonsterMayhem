/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

if(pressed){
	for(var j = 0; i < array_length_1d(deckNames); j++;){
		draw_text(x, y + 12*j, deckNames[j]);
	}
}