/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
if (visualState == VisualState.inactive) exit;
draw_self();
if(creator.currentMonster = monsterID){
	draw_sprite(sSelected, -1, x, y);
}

